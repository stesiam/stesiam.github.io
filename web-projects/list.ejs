```{=html}
<div class="grid gap-4">
    <% for (const item of items) { %>
    
    <% 
      // Clean the path to point to folder URL instead of index.qmd/html
      const cleanPath = item.path.replace(/index\.html$/, '').replace(/index\.qmd$/, '').replace(/\/$/, '') + '/';
    %>
    
    
    <div class="g-col-12 g-col-md-6">
        
        <div class="custom-card h-100 d-flex flex-column">
            <div class="img-wrapper">
              <% if (item.image) { %>
                        <img src="<%- item.image %>" alt="<%= item["image-alt"] || item.title %>">
                      <% } %> 
            </div>

            <div class="card-body p-4 d-flex flex-column h-90">
                <div class="mb-2">
                    <span class="badge-custom bg-tech">Python</span>
                    <span class="badge-custom bg-data">Analysis</span>
                </div>

                <h3 class="card-title card-title-fixed"><%= item.title %></h3>

                <p class="card-text flex-grow-1"><%= item.description %></p>
                
                
                <% if (!item.links) item.links = []; %>
                  <% item.links.unshift({ url: cleanPath, icon: "bi-journal-bookmark", text: item.appText }); %>
                  
                  <div class="d-flex flex-lg-row flex-column justify-content-center gap-2">
                  <% for (const link of item.links) { %>
                    <a href="<%= link.url %>" class="btn btn-primary mt-auto">
                      <i class="bi <%= link.icon %>"></i> <%= link.text %>
                    </a>
                  <% } %>
                  </div>
            </div>
        </div>

    </div>
    <% } %>
</div>
```