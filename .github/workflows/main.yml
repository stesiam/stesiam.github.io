name: Render babelquarto (quarto) website
permissions: write-all
on:
  workflow_dispatch:
  push:
    branches: gh-pages

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      deployments: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: pre-release

      # install libcurl, needed for curl R package
      # libfribidi-dev libharfbuzz-dev for textshaping
      # libxslt1-dev for xslt
      - name: Install R package deps
        run: |
          sudo apt update
          sudo apt upgrade -y
          sudo apt install libcurl4-openssl-dev libfribidi-dev libharfbuzz-dev libxslt1-dev libblas-dev liblapack-dev libpoppler-cpp-dev libfontconfig1-dev -y

      - name: Install R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.5.2"
          
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Configure Java for rJava
        run: |
          echo "$JAVA_HOME/bin" >> $GITHUB_PATH
          sudo R CMD javareconf

      - name: Install R Dependencies
        uses: r-lib/actions/setup-renv@v2

      - name: Render book
        run: Rscript -e 'babelquarto::render_website()'
        env: # Set the secret as an input
          BABELQUARTO_CI_URL: https://stesiam.com

      - name: Deploy to GitHub Pages
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./docs  # Quarto output directory

      - name: Deploy 🚀
        uses: actions/deploy-pages@v2
        with:
          cache-version: 1
