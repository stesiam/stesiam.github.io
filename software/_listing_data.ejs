```{=html}
<div class="grid pb-4">
  <% for (const item of items) { %>
    <%
      // Ensure link points to rendered Quarto article (folder URL)
      const cleanPath = item.path
        .replace(/index\.html$/, '')
        .replace(/index\.qmd$/, '')
        .replace(/\/$/, '') + '/';
    %>
    
    
    <div class="g-col-12 g-col-md-6">
      <div class="card h-100 border-0 shadow-sm custom-card-hover p-2" style="border-radius: 24px;">
        <div class="card-body d-flex flex-column">
          
          <h3 class="card-text"><%= item.title %></h3>
          
          <p class="card-text mb-4 small">
            <%= item.description %>
          </p>

          <div class="grid g-2 mb-4 mt-auto" style="--bs-columns: 12;">
            <% if (item.period) { %>
            <div class="g-col-6">
              <div class="d-flex align-items-center p-2 bg-light rounded-3">
                <i class="bi bi-calendar3 me-2"></i>
                <div class="lh-1">
                  <div class="label-caps"><%= item.periodText %></div>
                  <span class="small fw-bold text-dark"><%= item.period %></span>
                </div>
              </div>
            </div>
            <% } %>
            
            <% if (item.records) { %>
            <div class="g-col-6">
              <div class="d-flex align-items-center p-2 bg-light rounded-3">
                <i class="bi bi-layers me-2"></i>
                <div class="lh-1">
                  <div class="label-caps"><%= item.recordsText %></div>
                  <span class="small fw-bold text-dark"><%= item.records %></span>
                </div>
              </div>
            </div>
            <% } %>
          </div>

          <div class="mt-auto">
            <a href="<%= cleanPath %>" class="btn btn-primary w-100 rounded-pill fw-bold py-2 stretched-link">
              <%= item.button_text || 'Explore' %> <i class="bi bi-arrow-right-short ms-1"></i>
            </a>
          </div>
        </div>
      </div>
    </div>
  <% } %> 
</div>
```